import SwiftUI

struct RootView: View {
    var body: some View {
        // M0: the root screen is the design-system proof gallery.
        ComponentGalleryView()
    }
}

#Preview {
    RootView()
}
