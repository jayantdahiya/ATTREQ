# ATTREQ Web

The frontend lives at `apps/web` and uses a `src` layout.

## Structure

```text
apps/web/
├── public/
├── src/app/
├── src/components/
├── src/hooks/
├── src/lib/
└── src/store/
```

## Run Locally

```bash
cd apps/web
npm install
npm run dev
```
